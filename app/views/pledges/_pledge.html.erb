<tr>
  <th scope="row"><%= pledge.id %></th>
    <td><%= link_to pledge.reward.project.name, project_path(pledge.reward.project) %></td>
    <td><%= number_to_currency(pledge.amount, unit: '$') %></td>
    <td><%= pledge.reward.estimated_delivery %></td>
</tr>